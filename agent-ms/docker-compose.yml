version: '3.8'

services:
  agent-ms:
    build: .
    ports:
      - "8000:8000"
    environment:
      - GEMINI_API_KEY=AIzaSyDGYSPMB8AscSOcDG-A06bFEaXhjhCJMBU
    volumes:
      - .:/app
    restart: unless-stopped
